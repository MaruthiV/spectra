{
  "name": "@spectra/runtime",
  "version": "0.0.1",
  "description": "Browser-native speculative decoding for small open LLMs in WebGPU. Bridges mlc-llm's batch_spec_verify TIR op to web-llm.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.10.0"
  },
  "//": "Runtime deps (@mlc-ai/web-llm, @mlc-ai/web-runtime) will be added once the stubs are wired to real tvmjs APIs in Phase 1.5+. Stubs compile standalone today.",
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
